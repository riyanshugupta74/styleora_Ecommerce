services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: styleora_app
    ports:
      - "8000:80"
    env_file:
      - .env
    volumes:
      - .:/var/www/html
    restart: unless-stopped